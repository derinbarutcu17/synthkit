# Test Matrix

- Unit: parsing, chunking, citation mapping, contradiction heuristics, confidence heuristics, provider adapters, export formatting
- Integration: ingest -> synthesize -> inspect -> export
- MCP: tool listing, prompt listing, resource reads, no stdout pollution
- CLI: doctor, demo, JSON mode, exit codes
- HTTP: health, version, capabilities, project lifecycle, synthesis endpoints
- E2E: local demo workflow
- Eval: citation usefulness, contradiction recall, hallucination resistance

